machine: dev

wps:
  wps_tmpl_dir: "../../submodules/WPS"
  geog_data_path: "/tmp"  # dummy path

wrf:
  wrf_tmpl_dir: "../../submodules/WRF"

pipeline: "p_default"  # this needs to match a pipeline variable in pipeline.py
