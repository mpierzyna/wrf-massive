machine: hpc

wps:
  # WPS not done on hpc. Set to fake paths.
  wps_tmpl_dir: "/tmp"
  geog_data_path: "/tmp"

wrf:
  wrf_tmpl_dir: "/home/mpierzyna/.local/easybuild/RHEL9/2024/software/WRF/4.5.2-foss-2024a-dmpar/WRFV4.5.2"

pipeline: "p_hpc"  # this needs to match a pipeline variable in pipeline.py
